# Moviebook Backend

This folder contains the backend for the Moviebook app.

## Scripts

- `npm run server` starts the backend with nodemon.
- `npm start` starts the backend with Node.js.

## Notes

- Run backend npm commands from this folder.
- Frontend files belong in the sibling `frontend/` folder.
